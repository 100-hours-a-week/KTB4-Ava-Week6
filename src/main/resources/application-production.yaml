spring:
  datasource:
    url: jdbc:h2:file:/home/ubuntu/week6-data/week6db

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  h2:
    console:
      enabled: false

logging:
  level:
    org.hibernate.SQL: info